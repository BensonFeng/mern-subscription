import React from "react";
import { Navbar, NavItem, NavLink } from "react-bootstrap";

const Nav = () => {
  return (
    <Navbar>
      <NavItem>
        <NavLink>NavBar</NavLink>
      </NavItem>
    </Navbar>
  );
};

export default Nav;
